{
  "name": "drizzle-view-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Drizzle View workspace",
  "scripts": {
    "dev": "pnpm run dev",
    "build": "pnpm run build:view && pnpm run build:cli",
    "build:view": "pnpm --filter @drizzle-view/view build",
    "build:cli": "pnpm --filter drizzle-view build",
    "lint": "biome check",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0"
  }
}
